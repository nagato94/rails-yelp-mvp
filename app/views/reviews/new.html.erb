<div class="container">
  <h1 class="text-center">Reviews#new</h1>
  <%# tenho de colocar na ordem que aparece na url %>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Salvar review", class: "btn btn-primary" %>
  <% end %>
</div>
